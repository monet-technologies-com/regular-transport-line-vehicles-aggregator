# regular-transport-line-vehicles-aggregator

## 実行手順
```
pip3 install requests
cd regular_transport_line_vehicles_aggregator
export ENV=xxx  # [alpha-dev, dev, alpha-stg, stg, trial, prod]
export TOKEN=xxx  # 管理者webからjwt取得
python3 main.py
```
